{
  "name": "three",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean"
  },
  "devDependencies": {
    "vite": "^7.1.2"
  },
  "dependencies": {
    "@studio-freight/lenis": "^1.0.42",
    "three": "^0.179.1"
  }
}
